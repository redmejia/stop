import { images, pants, shirts } from "../shared/data";

export const initState = {
	images : images,
	pants : pants,
	shirts : shirts
}

export const Reducer = (state = initState, action) => {
	return state
}